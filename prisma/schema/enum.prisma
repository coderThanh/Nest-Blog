enum RecordStatus {
    DRAFT
    PUBLISHED
    PENDING
}

enum StoreProvider {
    LOCAL
}

enum GenderEnum {
    MALE
    FEMALE
    OTHER
}

enum PermissionScope {
    OWN
    SYSTEM
}

enum TokenType {
    PASSWORD_RESET
    EMAIL_VERIFICATION
}