model Permission {
    id         Int    @id @default(autoincrement())
    action     String
    permission String @unique
    module     String
    search     String @default(dbgenerated())

    roles RolePermission[] @relation("RoleToPermission")
}
